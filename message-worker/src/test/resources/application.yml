logging:
  level:
    br:
      com:
        brunno98:
          messageworker: DEBUG

application:
  queue:
    consumer-queue: 'foo'
    producer-queue: 'bar'

sqs:
  server:
    protocol: "http"
    host: "localhost"
    port: 9321
    contextPath: ""